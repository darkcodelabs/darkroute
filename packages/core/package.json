{
  "name": "@fwm/core",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "type": "module",
  "description": "Pure proximity + alert engine for DarkRoute. Zero React, zustand, browser or backend dependencies — node-testable geodesy, slippy tiles and the driving alert state machine.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "files": ["src"],
  "sideEffects": false,
  "scripts": {
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "4.1.11"
  }
}
