/**
 * @fwm/api-client — placeholder.
 *
 * This package is empty on purpose. A committed OpenAPI contract must exist
 * before client types are generated. Nothing here may be hand-written: a
 * hand-copied request or response type is exactly the drift a generated client
 * exists to prevent.
 *
 * Until the contract lands there is no client, and no caller should import
 * anything from here.
 */
export const API_CLIENT_PLACEHOLDER = true;
